//Objeto Math en JS

let resultado;

resultado = Math.PI;
resultado = Math.round(2.5); // Redondea convencionalmente hacia arriba o hacia abajo
resultado = Math.ceil(2.3); // Ceil rendondea siempre hacia arriba
resultado = Math.floor(2.3); //Floor redondea hacia abajo
resultado = Math.sqrt(144); // SQRT saca la raiz cuadrada
resultado = Math.abs(-144); // abs convierte un numero negativo y lo convierte en positivo
resultado = Math.min(3, 4, 5, 1, 12, 46, 3, 6, 6,9); // Nos muestra el minimo numero de un listado
resultado = Math.max(3, 4, 5, 1, 12, 46, 3, 6, 6,9); // Nos muestra el maximo numero de un listado
resultado = Math.random(); // Nos genera un numero aleatorio
resultado = Math.floor (Math.random()*30);

console.log(resultado);